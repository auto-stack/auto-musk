# Known Debt & Risks

> 复审（plan-archiver Step 2.5）登记的 workaround / 一致性遗漏 / 已知限制 / 未来增强。
> 每行一项，按严重度分级。新增条目请在对应分级表追加。
>
> **a2r 转译器限制统一登记在 auto-lang `docs/plans/391-a2r-parity-debt-from-musk.md`**；
> Plan 391 的 D1-D6 已全部闭环并合入 auto-lang master（2026-08-06），
> auto-musk 侧对应的 .at 变通已逐项去除（D1/D2/D3/D5 + 此前的 wiki 两项）。

严重度图例：
- 🔴 **高风险** — 特定（非平凡）条件下可能引发 UB / 数据损坏
- 🟡 **一致性遗漏** — 功能正确，但代码未达计划自身的一致性目标
- 🟢 **已知限制** — 设计决策（非 bug），值得记录
- 📋 **未来增强** — 留待后续计划的优化 / 清理

---

## 🟡 一致性遗漏

| Plan | 描述 | 参考 |
|---|---|---|
| 027 | **SSE `tool_result.status` 恒硬编码 `"success"`**（027 提交④许诺登记但漏登，2026-08-24 普查补登）：`stream_event_to_json` 的 Tool 分支不区分 error，错误语义靠双轨前端嗅探 result 前缀（`[security denied`/`[tool error]` → failed：web `useForge.ts` + gen `forge_store.at`）。**解除时机**：musk 适配 auto-ai 027 content/details 分离（修编译红）时顺带落 status 真字段 + error envelope，前端去嗅探。 | `backend/crates/musk/src/server.rs`（stream_event_to_json Tool 分支） |
| 030/038-040 | **merge 沉淀后归档移动不回写 specs.json 的 `file` 指针**（系统性，2026-08-24 普查发现）：四计划归档后其 28 条 SpecItem（每计划 P0NN-1..7）仍指 `docs/plans/0NN-*` 旧活动路径——溯源链接全断。**已修复**（2026-08-24 文本替换为 `docs/plans/archived/`，JSON 校验过）；根因在 merge 时写指针、后续 archive 移文件两步无联动，建议 merge skill 或 archive 流程补"归档时回写 file 指针"。**2026-08-29 复发与处置**：P046 七条（端点式 merge 后归档未回写）已文本回写；P045 沉淀改为**直写归档路径**（manual merge 先例，后续 merge 照此）。 | `backend/.autoos/specs.json`（运行时账本，gitignore） |
| — | ~~**auto-ai pi-parity 027/028 跨仓漂移致 musk 编译红（2026-08-24 实测 33 错）**~~ **✅ 机械适配完成（PLAN-042 T1，ad5b2a4，2026-08-24）**：26 处 execute 签名迁 `ToolOutput` + 31 返回点 `text()` 包裹 + 测试取 `.content`；全量测试 0 失败，主线编译恢复。**剩余（归 PLAN-042 T3-T8）**：真 details 填充（edit diff/read 截断/run_command 全量路径）、事件链透传（relay RunEvent/conversation 持久化/SseEventDto 结构）、前端渲染；SSE `tool_result.status` 真字段仍缺（hw 轨已透传 details，status 恒 success + 前端嗅探维持——027 条债务未解除）。 | `backend/crates/musk/Cargo.toml:17` / [PLAN-042](042-pi-parity-tooloutput-migration.md) |
| 033 | **W1 归档路径绕过状态机**：`move_to_archived`（plans.rs:520）直写 status 而不经 `can_transition`——计划文本"transition(Archived)+rename"与"状态机去掉 reviewed→archived 边"自相矛盾，实现取后者。风险：未来 `transition()` 若增加副作用（事件/钩子），终态路径不触发。改进方向：显式建模"系统迁移"（force 参数或独立类型）。 | `backend/crates/musk/src/plans.rs:520` |
| 033 | **W2 渲染层仅代码级验证**：MetaBlock 布局/徽标中文/按钮组换行只有代码+单测证据（复审时内置浏览器 webview 无法挂载），无人眼/截图验证。补验：起服务人工过目（约 5 分钟）。**2026-08-25 重试记录**：服务 :8580 在跑（新二进制 + 新 dist），但会话内置浏览器再次报 "browser guest not attached (webview not ready)"——同一环境限制，两次不同日期复现；浏览器冒烟仍需用户在普通浏览器中执行（`http://127.0.0.1:8580` → Plans 视图 + 聊天工具卡 details 区，可与 PLAN-042 手工冒烟合并一次做）。 | `web/src/views/PlansView.vue:163` |

## 🟢 已知限制

| Plan | 描述 | 参考 |
|---|---|---|
| 041 | **web 轨退役残余——6/6 全闭(2026-09-01 观察期提前收口→永久冻结,用户裁定归档;原时间门 09-03,窗口 08-27→09-01 零 P0/零回滚,web/FROZEN.md 已更新)**：①✅编辑器组五件落地(specs_editors.at;web AutoDownEditor 本即 textarea stub,@autodown/editor 前提失效重裁定,真身归 PLAN-038);②🔶URL 路由桥接形态落地(viewstate_router.ts + App/specs_view 接线;T7 勘察裁定端口层——auto-lang routes 为 hash-router 语义;残余:popstate 视图级经 rail 点击桥接、会话中 detail 级 popstate 不回放,浏览器手动实测待用户);③✅detail 组 Goal/Review/Test/Report 逐件移植+detail_kind 分派+GoalDetailModal(Api/Plan web 零消费不移植);④✅观察期提前收口(见行首)——冻结转永久;⑤✅StatusTransition select 化(NativeSelect ext 逃生,schema 无原生 select;对拍 N12 撤除);⑥✅i18n 358 键=web 全集+gen-only(T13 测试资产原落 gitignored gen/ 未入库——已 git add -f 入库,vitest 经 npx@2.1.9 锁版)。**merge 落账残余(2026-09-01 归档时入册)**:⑦🔶GateBanner(SpecsView 内嵌审批横幅)形态差异未移植——审批面经 GateInbox 域(chats 面 Secretary/GateCard)覆盖,按需补;⑧🔶OnboardingDialog 新建工程向导形态未移植——gen workspace 流程(WorkspaceSelector)吸收,按需补。**对拍 30/30 exit 0**。 | `docs/plans/archived/041-web-track-retirement.md` / `web/FROZEN.md` |
| 041a | **auto-lang 侧缺口——六项全部闭账(2026-08-26 Phase 5 收口,auto-lang b5e392c16)**:①✅原生元素直通(schema+13 件+map_tag arm,N16/N17 撤);②✅重定性非缺口(trim_right 规范名);③✅slot/teleport 入 schema+S002 清零+**musk 摘除 --lenient(strict 零 flag 全绿)**——配套 R006 双修(索引/id 键回退+嵌套 Dot 键提升)与 fn-only 文件 strict 降级;④✅动态 Regex pattern 转译(new RegExp(expr) 包装);⑤✅v-model 折叠 prop_names 守卫(prop 目标单向);⑥✅handler 体内句首 . 派发不再并入前一语句(in_on_body 禁用链合并)+括号表达式简写消歧(`(` 后首 token 为 Dot)——事件绑定嵌套调用复核本就可用,`to` 保留字报错优化登记不做(低收益)。musk 侧消费:specs_view `{  {` 归一、session_info text-if 中转。**唯一遗留:musk 侧曾以 tab 元素/native workarounds 落地的代码不回改(现役稳定)**。 | `docs/plans/archived/041-web-track-retirement.md` Phase 5 / auto-lang b5e392c16 |
| 044 | **数据层域退役排期（桥接形态,Phase 3 样板 auth 域已闭）**：specs 域（specs_load/overview_of/drift/rebuild/related_of/upsert_of/transition_of/delete_of + specs_read/list/write/update/write_goals——store 层为手写 Rust specs.rs,需 .at 化重写后才可走模板第 5 步深化；桥接消费面已通）；wiki 域（ws_wiki_* 13 extern + wiki_raw_*——store 为手写 wiki.rs,同 specs）；relay 域最重（relay_run_* 12 + task_plan_* + driver/bus 30+ extern,SSE/timer 面）。默认终点 = 模板第 4 步双轨等价（部分已达成：relay_runs_list/task_plans_list/professions/flows 已桥）；第 5 步纯化按需渐进。模板见 [PLAN-044 附录](044-vm-backend-bridge-and-data-autolization.md)。 | `backend/crates/musk/src/auto_generated/extern_impl.rs` / `src/vm_backend.rs`（注册表） |
| 442 | **VM 轨 platform/composables adapter 显式降级**（Phase B，2026-08-25 落地；044 收口注：VM 后端桥接形态 2026-08-27 已通，`ports/platform.vm.at` 三桩中 setupAuthFetch 仍为 no-op——VM 前端 Http 走 native 直连无认证头注入，鉴权 API 消费面出现时需补——**✅ 已解除(2026-08-26,auto-lang 446-E4)**:Http.set_default_header/set_default_query/clear_default_auth 三 native 落地,musk 侧 platform.vm.at 实装 + login Submit/App Init 重注入(web adapter no-op;边缘:Me 内部 Logout 的头滞留见 platform.vm.at 注释)）、platformRunRelayCommand 降级为留痕打印（跨 RelayStore/ForgeStore 接线待多 store 放开，与 web 侧 D 组同源——**BLOCKED 登记(PLAN-046 T9)**:auto-lang A1 多 store 消歧未合入,合入后由后续批次直连双 store）；`ports/composables.vm.at` 的 useGateRouter no-op（VM 导航走视图状态机）。icons/renderer/upload 三域 VM 目标为视图层组件引用，无 handler 符号面，无需 adapter（严格模式探针实证）。dom/location 全局面由 auto-lang 442 native 桥承接（2774-2779/2784）。 | `src/front/ports/platform.vm.at` / `src/front/ports/composables.vm.at` / [auto-lang 442](../../auto-lang/docs/plans/442-cross-platform-closure.md) |
| 045 | **VM 前端链接门禁复绿后的 auto-lang 侧移交清单(2026-08-26)**:①handler 重写器 try/catch 缺臂 + ②循环回跳 i16 回绕(>32KB 合成模块)——两项已修(auto-lang 分支 plan-446-try-rewrite,K0/K1 登记);③obj 接收者方法族零 VM native(obj.slice/obj.find 链接死;musk 已规避——[]str 类型注解/手扫循环;上游补 obj 族或文档化)——**⏳ 转入 [PLAN-046](046-vm-debt-retirement.md) 同步批次**(T1 判与 auto-lang plan-447 在途改动集撞文件挂起;musk 回撤位点清册=046-T4);④普通 fn 直读 store state 不可链接(ensureAssistantMsg 类已拆纯 fn 规避,上游待议)——**✅ 已规约化(2026-08-27,PLAN-046 T11)**:`src/front/README.at-conventions.md` R1 纯 fn 形态约定,不修链接器;⑤32KB/模块 i16 偏移上限(架构约束,widening/分模块为长期项)——**✅ 观察门禁落地(2026-08-27,PLAN-046 T10)**:vm-link-probe 出 `[probe]` 体积行(WARN 90000/FAIL 131072 env 可调);实测 link 合计 60666 bytes 探针 PASS——K1 isize 化后 32767 非硬红线,单模块精确记账仍需上游 per-module 面(入 046 同步批);⑥探针调用勘误(feature=ui-iced 非 ui-interpreter)。musk 侧常驻门禁:`scripts/vm-link-probe.cmd`。 | `docs/plans/045-musk-vm-link-regreen.md` / [auto-lang 446 K0/K1](../../auto-lang/docs/plans/446-vm-backend-os-config-field-report.md) |
| 046 | **VM workaround 债务清偿的同步义务清单(2026-08-27 立项即部分落地)**:已完成=探针体积门禁(auto-lang master e3abde1ba bytecode_len accessor + `[probe]` 行 + musk 门禁阈值)、认证滞留协议固化(auth_store.Me 注 + platform.vm.at 头注;Me 现零派发点暴露面为零)、规约文档(R1-R3)、T4 规避位点清册(回撤类4/保留定型类5)。**待同步批次(2026-08-27 更新:447 已合、冲突解除;A 件半程入档(2026-08-27 用户裁定提前并入)=auto-lang master `0737c26f3`(obj 族基线:注册/路由/链接三关+shim 实体,musk 三门禁+上游 lib 3216 双认证绿),残余=动态值运行时语义三缺口【Option 无类型路径传播/闭包×GET_FIELD 协作/返回值静态型别】,规格见分支提交体;原 WIP 三缺口已由 plan454-E5 收口(db22358ec,测试 4/4);musk R1-R3 已回撤自然形态、R4 改判保留——vue-TS 对 Object.values 元素推 {},「ts_adapter 显式类型包装」立 A′;B 件✅全闭(2026-08-27 本会话微批合入 auto-lang master:storage_host_publish + startup/双 resize 漏斗发布;musk platformViewportHeight 已改读 KV,desktop 冒烟随观察期))**:A)obj 方法族 native(obj.find/Object.values);B)window height native → platformViewportHeight 真值化;C)Http 响应 status 的 .at 可读面 → 会话过期 401 兜底(auth 协议注记已备);D)per-module 字节记账(体积门禁精细化)。**独立阻塞件**:relayRunRelayCommand 完整接线 BLOCKED 于 auto-lang A1 多 store 未合入——**A1 已于 2026-08-27 合入**(b0ad9dc96,446 批二):musk 侧引发八处 store 调用限定化(1bcc820)+vue ts_adapter facade 发射配套微批(auto-lang 1487b5c5d,TDD lib 3223 绿);本项接线解除阻塞,归后续批次。 | `docs/plans/046-vm-debt-retirement.md` / [auto-lang 447](../../auto-lang/docs/plans/) |
| 047 | **VM 首跑运行期移交清单(2026-08-27 execution_done;活体运行达成=iced 窗口+GPU 渲染+MCP 无头交互取证,详见计划红清单节)**:**UPSTREAM→auto-lang 两件**:①视图级派生绑定 for 源不查 computed 表——`aura_view_builder.rs resolve_iterable`(~262)仅 state 读,EDGE-16 第五层 computed 求值(~117)只接 prop 面;chats_view:74 filteredMessages/lastMsgId 消息列表空渲染(每帧 WARN 刷屏),侧栏 store 直读不受影响;②ext-link 装载器 fn-only——`load_ext_imports_for_vm` 只把 adapter 内纯 fn 别名为符号,widget 不进视图 registry → renderer.vm.at 同名 Markdown 文本降级组件就绪未激活(消息正文空,musk 侧文件已入库 c66dbe7)。**BLOCKED(第二梯队立项)**:R8 数据桥选型——auth/chats 走 `use back.api`,merged 模式宿主无挂载(pac.at 无 back.project/无 src/back),登录→chats 数据链路不通;候选=宿主 cdylib 桩载(061 形态接后端 crate) vs AUTO_VM_MERGE=0 分离模式接 044 VM HTTP 桥;auth natives 注头链路本体已就绪。**~~R8~~→✅ 已解除(2026-08-28,PLAN-048)**:契约通路定型 AUTO_BACKEND 门(api_over_http 第二开关,不触发 split 后端拉起),上游两补+活体验证,详见 048 行。**DEGRADED 四域**:icons(lucide×48 空框可用)/upload 家族/viewRouter·vs*(web URL 桥,VM 状态机自足实证)/Markdown 文本降级(随②激活)。**OPEN 观察**:R7 `${runId}` 模板字面残显(relay_run_box,未定罪)、R9 未认证直达 chats——**✅ 已修复(2026-08-28,PLAN-048 A 线 L1)**:根因=视图条件求值不接 store computed 表+None 字面量不可解析(eval_condition_with LHS read_state 失败即假);上游三处修复(auto-musk-dev 0f3dd82dc→master a42ed3909)后 LoginPage 正常挂载、登录后门翻转实证。常驻门禁:`scripts/vm-first-run.{mjs,cmd}`(等价 VM 编译链接红线,fatal 五类全程 0)。**merge 终验补齐(2026-08-27)**:正式探针主检出两连绿(60870/60846 bytes,P3/P4;此前 P1 为 rustc STATUS_ACCESS_VIOLATION 编译器瞬态崩溃,与 046 复审所录文件锁类环境抖动同源,复跑即愈,非语义红)+A1 落地引发八处 store 调用限定化(1bcc820)+vue facade 配套微批(auto-lang 1487b5c5d);build strict/vitest 23+1/对拍 30/30 全绿。归档=e8ee 系(见 git log)。**G1(SSE 无 VM 形态)勘察定型(2026-08-28,PLAN-048 T6)**:三方案对比(轮询/WS native/后端增量端点)+ iced 渲染器 shell-SSE 桥泛化新候选,推荐排序=阶段1 轮询(0.5-1 日,零上游)→阶段2 SSE 桥泛化(2-3 日上游,零新 native,与 UPSTREAM② 同批解除流式渲染双降级);报告入册 `docs/specs/reports/048-sse-vm-survey.md`,流式实现归下一计划。 | [PLAN-047](047-musk-vm-first-run.md) 首跑红清单节 / auto-lang aura_view_builder.rs · lib.rs load_ext_imports_for_vm |
| 048 | **VM 数据桥接线移交清单(2026-08-28 execution_done)**:**✅ 契约通路定型+落码+活体验证**:门=AUTO_BACKEND(api_over_http 第二开关,lib.rs:3546;`--no-merge` 路线判死——rust_ui.rs:2437 会为 musk 拉生成式后端);上游两补(auto-musk-dev 63e20b6ec→master 9f666d1ac)=①`emit_api_http_call` 支持 `{param}` 花括号路径模板(brace 优先,`:param` 兼容)②`simple_http_json`(get_json/post_json 底层)应用 446-E4 默认头/查询——此前契约调用裸发;活体=LoginPage 填表→Submit→auth_login POST→token=后端 JWT→LoadSessionList→chats_list_sessions GET→session_list 10 会话入状态(auth_login/chats_list_sessions 两条契约活体往返;MCP 截图+状态双证在案 tmp/plan-048-evidence,musk worktree)。**musk 侧三修**:①mention_input `store.Init()`→`AgentConfigs.Init()`(446-A1 撞名清偿);②app.at Init 代派 ForgeStore.Init(子部件 Init 不派发 workaround);③forge_store Init 缺席清写 token 修正(扁平化单状态 token 撞名,`tk ?? ""` 覆写认证门 nil)。**上游 A 线 L1(登录墙根因,授权全量修复)**:eval_condition_with 接 computed 回退+Expr::None/Nil/Null 臂+DynamicComponent 并入 store-as-child computed(0f3dd82dc→a42ed3909)——R9 登录墙缺位修复,LoginPage 正常挂载、登录后门翻转。**vue 跨 store facade**(5b9362af→7152d6ad4 系):extract 补 XxxStore 限定引用扫描+deps[1..] 独立 facade 常量+ts_adapter store_facades 映射——App.vue/auth_store 跨 store 调用 TS2304 清偿。**DEGRADED 数据面清单(30 fn 代码通路已通、触发面/接线未通,挂账)**:chats_approve/chats_reject_all/chats_fork_session/chats_navigate_session/chats_delete_session/chats_delete_all_sessions(forge+chats_view 域);plans_list/plans_get/plans_create/plans_update/plans_transition/plans_archive/plans_merge(plans 域);specs_list/specs_overview/specs_save_item/specs_delete_item/specs_rebuild_relations/specs_tree/specs_get_file(specs 域);wiki_list_pages/wiki_get_page/wiki_create_page/wiki_update_page/wiki_delete_page/wiki_search/wiki_tree/wiki_raw_tree/wiki_raw_delete_file/wiki_raw_mkdir(wiki 域)。注:MVP 7 fn 中 chats_create_session/chats_get_session/chats_send_message/auth_register/auth_me 代码通路已通(同臂),活体触发面待 L3。**UPSTREAM→auto-lang 四件(本计划新增)**:①slot 内容不渲染——NavSidebar slot(name:list/actions) 子树在 VM 视图缺失(会话项/新建删除按钮不可达);②子→父回调 emit 无通用路由——MentionInput `.send` 不派发 onsend(renderer.rs:7656 自注"非通用 emit 修复",ash-gui 为 widget 特定硬编码),发送输入条在 VM 轨不可达;③boot-Init 期异步 HTTP 结果无人恢复嫌疑(Init 链跑至 LoadSessionList 无报错无结果,静默挂起形态;用户期 handler 泵已实证正常);④视图文本裸调用臂缺失——`text t("nav.chat")` 恒空(resolve_expr_to_value 只会接收者方法调用,Ident 名裸调用落入 `_=>None`;ext 别名纯函数直调返回值亦型别错乱,t_now 探针恒返 int 5,同 046「返回值静态型别」缺口族)→ 导航/品牌文案已字面量化缓解(afc1f26;代价=locale 切换不跟改,正规解=上游文本调用求值臂);同族未修=lucide 图标空框(047 DEGRADED);**样式段已核销(2026-08-28,PLAN-049)**:「rail-tab 等自定义类定义在 web-only inject_styles.ts」三层根因之第三层已由 PLAN-049 单一样式源收敛消除——全组件自定义类迁 .at 内联 tailwind 工具类(VM class.rs 解释同一类串),inject_styles.ts 退役拆 web-only.ts,style-parity 对拍门禁上线(58 用例 diff=0);「空白按钮+宽度不对」随迁移消除(rail idle 态 shadcn Button 默认底透出亦已修,bg-transparent)。**OPEN 观察**:a) VM 前端进程 ~1.5-4.5 分钟静默退出(exit 1,无 panic 输出;AUTOUI_MCP_DISABLE=1 禁心跳无效;非 CloseRequested 干净退出)——退出源未定罪;B) ~~VM `localStorage.*` 族会话级内存+文件族 Storage.* 惰性加载不挂 raw 读点→KV 会话恢复在 VM 轨结构性失效~~ **✅ 已修复(2026-08-30,登录表单批次)**:raw localStorage 家族(storage_raw_get/set/remove)补齐 load-once+write-through(auto-musk-dev 4c2b2ff5d→master),muskrat jwt/登录凭据跨重启恢复实测通过(重启直达登录页预填 admin/admin;backing file=%TEMP%/auto-vm-storage/<cwdhash>.json)。**残留风险(升级为观察项)**:KV 恢复使 boot 直达聊天视图/登录后进聊天视图,在 **debug 构建**踩 RC canary(engine.rs:1532 string tombstone/rc.rs:503 use-after-free→PoisonError 级联退出)——canary 为 debug_assertions 仪器化,release 构建无恙(实机验证通过);根因即 plan051 P2 追加三留档的「每帧 resolve 经 call 链返回堆引用未 retain」上游 RC 语义债,修复归上游 RC 语义先行项,期间 VM 轨日常请用 release 二进制;c) musk serve 无请求日志中间件(INFO 级仅启动行)→契约调用的服务端观测需 TraceLayer 或副作用回读;d) @autodown 包名漂移(上游 schema.rs:2847 起 engine,vendor 仍 vue)——build 期经 node_modules junction `engine→vue` 桥接(会话级),持久修复=package.json/vendor 更名跟进。**常驻门禁**:四门禁全绿(build strict/vitest 23+1/对拍 30/30/探针 PASS 60878 bytes)。 | [PLAN-048](048-musk-vm-data-bridge.md) 执行步骤/待澄清节 / auto-lang vm/codegen.rs emit_api_http_call · vm/ffi/stdlib.rs simple_http_json · aura_view_builder.rs eval_condition_with |
| 049 | **双轨样式收敛移交清单(2026-08-28 execution_done)**:**✅ 单一样式源落地**:.at 内联 tailwind 工具类为唯一样式源,VM 轨 class.rs 解释同一类串;迁移面=T4 导航栏(048 试点收尾+viewstate_router .rail-tab 选择器断链修复)/T6 会话壳九组件(nav_sidebar 宽度参数化 width_class/chats_view/chat_message 角色条件类串等价 :has()/gate_card style{} 块迁移代表点/mention 双件)/T7 三域(plans/specs/wiki+session_info 寄居规则随域迁走);inject_styles.ts 退役→余量(全局段+web-only 增强)拆 inject_styles.web-only.ts(D4),platform.web.at 改指。**style-parity 门禁上线**:`scripts/lib-parity/style-parity/{run.mjs,norm.json,fixtures/cases.json}`——web 侧 tailwind 生成 CSS 静态匹配 vs VM 侧 class.rs dump(auto-lang plan449_style_parity_tests)逐属性 diff,终态 58 用例/422 token diff=0;白名单(hover:/focus:/transition 等变体+border 单侧族/underline/rounded-[20px] 等 iced 无对应项)报告计数不判失败。**上游 D3 已修并回(auto-musk-dev→master 3309909a8)**:①p/m 族 0.5 步进分数值(px-2.5 等,Pixels(N*4px) 对齐 gap 先例)②items-baseline 降级臂(ItemsStart);dump mapper 补 directional rounded 八向臂(bc5c4d06f)。**视觉验收实绩**:rail idle 态 shadcn Button 默认 bg-primary 透出(048 遗留)修复(bg-transparent);登录/会话壳暗色实拍存档(screenshots/);plans/specs/wiki DOM 快照实证。**二批挂账(待澄清①降级,不影响退役)**:余 31 处组件 style{} 块(specs_leaf×4/specs_editors×5/specs_detail×8/specs_category/errand_card/questionnaire_card/generic_tool_card/secretary_message/streaming_table/report_card/relay_run_box/raw_preview/task_plan_card/settings_menu/workspace_selector/think_block/agent_avatar)——web scoped 生效如旧,VM 无映射如旧,迁移归二批计划。**像素级长杆(登记不闭,L3)**:浏览器 flow vs iced row/col 布局引擎差异;文字度量差异;单侧边框(iced 均匀边框)/悬停态/绝对定位/transform/动画——白名单逐项在 norm.json。**上游 consume 记录**:b26b61fd0(markdown→@autodown/engine 包名迁移)未消费——musk 以 vendor/@autodown/engine shim(同 vue 0.2.0 实现,零行为变化)桥接,**替代本表 048 行 OPEN-d 的会话级 junction 桥**(持久化),真实 engine 消费另立计划后拆 shim;plan442 探针增 MUSK_APP_PATH env(176fe7da4,worktree 布局门禁真跑)。**环境注记**:验收临时账号 zcode-style-check(共享 demo 后端,未动工作区数据);IAB localStorage 登录态已清,用户需重登。**常驻门禁**:build strict/vitest 23+1/phase1-leaves 30/30/vm-probe PASS 60890B/style-parity diff=0 全绿。 | [PLAN-049](049-dual-track-style-convergence.md) 执行步骤/MIGRATION.md / auto-lang ui/style/class.rs 分数间距+items-baseline · plan449_style_parity_tests · scripts/lib-parity/style-parity |
| 026 | **手测 :3334 chat 气泡未补**（标准 6/步骤 5.5，当时 gen build 未全绿留待）：核心修复已由 a2vue fixture 008/009/010 + 生成物断言保证，后被 PLAN-028 双轨 148 项对拍 + 031+ 浏览器冒烟实质超越，未单独补验。低优先——可在 041 解冻后的生产切换冒烟（五视图 + 编辑器）时顺带闭。 | `docs/plans/archived/026-codegen-chat-defects.md`（标准 6） |
| 040 | **DEBT-040-1 前端流式进度 E2E 未执行**：tool_update 流式渲染组件级全绿，但"起 musk serve + 真实 LLM 会话跑长命令人工观察"的端到端冒烟待用户执行。 | 040 计划 §遗留 |
| 040 | **DEBT-040-2 Windows 进程树终止兜底**：`taskkill /T` 覆盖多数场景；若发现跨控制台分离的漏网进程，按 040 风险节评估 Job Object 兜底。 | 040 计划 §遗留 |
| 040 | **DEBT-040-3 Unix killpg 无运行验证**：仅代码审查（开发 host 为 Windows）。 | 040 计划 §遗留 |
| 024 | **`/api/plans` 走 hw 路由（非 ag 轨）**：a2r 转译器漂移（`auto trans specs.at rust` 产物含未被 nativeize 清理的 `a2r_std` 引用、多余 `.clone()`、分号风格差异），计划 §3.6 允许的逃生舱，功能等价（API 全链路 + 测试绿）。待 a2r 转译器对齐（auto-lang 433 a2r 闭环）后切回 ag 轨。 | `backend/crates/musk/src/server.rs:134` |
| 024 | **derive_statuses Rule 1 移除**：Goal→Implemented 自动推进规则依赖已移除的 plans section（plans 独立为 PlansStore）。Goal 需手动 transition 到 Implemented；Rule 2（Implemented→Verified）保留。 | `backend/crates/musk/src/specs.rs`（derive_statuses） |
| 024 | **`migrate_legacy()` 未独立实现**：计划 §5.2 列了独立方法，实际用 `update` 注入 frontmatter 行为替代（+ 测试，plans.rs migrate_legacy_injects_frontmatter）。旧格式容错可用（无 frontmatter → drafting）。 | `backend/crates/musk/src/plans.rs` |
| 038 | ~~**auto-icons 渲染层降级**（T9 canary 实证 2026-08-23）~~ **🔶 解除条件已达成,待实现（2026-08-23 canary 重放）——但 2026-08-24 范围复核：A4 仅覆盖静态 svg 树**：auto-lang 442 A4 的 svg 支持（vue.rs `is_svg_element` 分支）只处理**静态**元素树（svg 子树上的动态属性绑定被静态化才保语义）；而 Icon widget 需按 icons_data.at 的**逐图标动态元素序列**（元素数/标签不同）生成子树——当前语言能力仍不可表达。**可选路径**（未裁定）：(a) 生成器产 52 个静态 svg widget + Icon(name) match 分发（零语言改动，生成物量大）；(b) platform 协议挂载（同 markdown/StreamingRenderer 模式，Vue 平台实现挂 gen/src/platform——与 041 web 轨退役的 lucide npm 绑定处置属同一决策面）。数据层不受影响始终已交付：`src/front/lib/icons_data.at`（52 图标）+ 数据对拍全等（`scripts/lib-parity/icons.mjs` 降级态）。**建议归入 041 Phase 1 裁定**，不作为独立小任务承接。 | `src/front/lib/icon.at`（stub）/ [442 A4](../../auto-lang/docs/plans/442-cross-platform-closure.md) |
| 018 | `task_plan.at` C1：`impl TryFrom<Node>` trait impl → `static fn from_node`（Auto 无 trait impl 语法）。**a2r 计划391 D6 已让 `impl Trait for Type` 报清晰错误**（不再静默反转），但 Auto 语言层面的 trait impl 支持仍是未来设计决策。parity 分别调 hw `try_from` / ag `from_node` 比行为。 | `auto-src/task_plan.at:272` / [391 D6](../../auto-lang/docs/plans/391-a2r-parity-debt-from-musk.md) |
| 018 | ~~`app_config` 的 `AAID_URL` env 覆盖在 a2r 产物中缺失~~ **✅ 已修复（Plan 021 B1，2026-08-07）**：`app_config.at` 的 `effective_daemon_url` 现读 `env::var("AAID_URL").ok()`（与 hw file < env < default 一致；391 D4 让方法链可解析）。`parity_app_config.rs::parity_effective_daemon_url` 断言两侧行为一致。 | `auto-src/app_config.at` |
| 018 | ~~`wiki.at` TreeNode file 节点 `modified = None`~~ **✅ 已修复（Plan 021 B2，2026-08-07）**：`wiki.at` 新增 `file_modified(entry)` 辅助 fn（`modified().ok() → duration_since(UNIX_EPOCH).ok() → let secs u64 = as_secs()` 抑制 cast）；build_tree file 节点 `modified` 取真实 mtime。`parity_wiki_http` 逐键比对（含 modified）。 | `auto-src/wiki.at` |
| 018 | a2r 输出验证器对 specs.at 报 `unbalanced parentheses (depth: 1)` 警告（编译通过、测试通过，疑为字符串字面量内括号被误判）。非阻断，记为待查。 | `auto-src/specs.a2r.rs` 转译输出 |
| 019 | ~~ag 流式 handler 即时错误走 200 SSE + error 帧~~ **✅ 已修复（§6.1，2026-08-06）**：`run_stream_handler`/`workflow_run_stream` 现在在 `mpsc_channel()` 前前置 `mode_exists`/`workflow_exists` 校验（与 hw fail-fast 一致），坏 mode / 坏 workflow → 400 JSON。契约测试 `ag_run_stream_bad_mode_returns_400` + `ag_workflow_run_stream_invalid_workflow_returns_400` 锚定。 | `src/auto_generated/server_stream.rs`（前置校验）/ `extern_impl.rs`（`mode_exists`/`workflow_exists`） |
| 019 | ~~conversation_stream 的 broadcast Receiver 存 side-table，客户端断开时条目不回收（累积泄漏）~~ **✅ 已修复（§6.2，2026-08-06）**：`conversations_subscribe` 返回 `BroadcastSub`（rx 包 `Arc<Mutex<Option<Receiver>>>`），被 `conv_event_stream` owned → stream drop（客户端断开）→ Arc 归零 → rx 析构。不再用 registry 存 receiver。测试 `broadcast_sub_drop_reclaims_receiver` + `conversation_stream_drop_reclaims_receiver` 断言 receiver_count 回落。 | `src/auto_generated/extern_impl.rs` `BroadcastSub`/`conversations_subscribe` |
| 019 | mpsc channel 缓冲 64 条：run/chat/workflow 流在流建立前爆发超过 64 事件时 `try_send` 静默丢帧。**与 hw 完全等价**（hw 三 handler 同样 `channel::<Value>(64)` + `try_send`，`server.rs:367/618/904`），非缺陷、无需修复，仅登记备忘。 | `src/auto_generated/extern_impl.rs` `mpsc_channel` / `server.rs:367,618,904` |
| 020 | **a2r spec trait 无 supertrait 语法（Send+Sync）**：`TaskPlanExecutor` 等 spec trait 无法声明 `Send + Sync` bound → 生成的 `execute` future 非 Send,`relay_task_plan_start` 无法用 `tokio::spawn`,改用独立线程 + current-thread runtime `block_on`(future 全程不跨线程)。行为与 hw `tokio::spawn` 等价(后台执行 + HTTP 立即返回),非功能阻塞。**根因在 auto-lang 侧(spec 语法 + codegen 决策,类比 391 D6),完全未在 auto-lang 登记** —— 待 auto-lang follow-up 立项解除;解除后可把 spawn 路径改回 `tokio::spawn`。 | `auto_generated/extern_impl.rs` `relay_task_plan_start`(独立线程 block_on)/ `auto_generated/task_plan_engine.rs` `TaskPlanExecutor` spec |
| 020 | **a2r 借用版本漂移(task_plan_engine 既有块)**：Plan 020 Phase H 期间发现,当前 auto-lang master 的 a2r 对 `self.field`(`String`)在 extern 调用点发射 `self.field.clone()`(owned),而之前提交的产物发射 `&self.field`(借用);`@T` 实参的 `&` 注入也有差异(`&&handoffs` vs `&handoffs`)。这导致 task_plan_engine.at **既有**块 re-transpile 与 `auto_generated/task_plan_engine.rs` 有 drift(编译/测试仍绿,仅 codegen 风格差异)。Phase H 新增的 `RelayTaskPlanExecutor` 块与既有无关、零 drift。**非阻断**;待 a2r 借用启发式稳定后,统一 re-transpile 全模块消除 drift。 | `auto-src/task_plan_engine.at` / `auto_generated/task_plan_engine.rs` |
| 022 | ~~**Phase 7c RelayCard 为状态展示版**~~ **✅ 已修复（B 类批3）**：`RelayRunBox.vue` 替代 RelayCard 状态展示版，完整交互（subscribeToRun 实时日志 SSE + resolveGate 审批）；useRelay/useEventRouter/useGateInbox 逃生舱引入；`/relay`//`/superpower`//`/spec1` 命令路由（relay_commands.ts）。 | `src/front/components/RelayRunBox.vue` / `src/front/composables/useRelay.ts` |
| 022 | ~~**Phase 7c professionOptions 用内置默认列表**~~ **✅ 已修复（B 类批1）**：useAgentConfigs 逃生舱调 `/api/forge/relay/professions`（替代空 stub），MentionInput 优先用动态列表（DEFAULT_PROFESSIONS 仅作 fallback）。 | `src/front/composables/useAgentConfigs.ts` |
| 022 | ~~**Phase 7c 流式 draft 走静态 markdown 渲染**~~ **✅ 已修复（Phase 7.3 续）**：流式 draft + assistant 消息现均用 `StreamingRenderer`（逃生舱整体引入 useStreamingDocument 增量 JSON 解析 + StreamingTable + markstream-vue），不再走 MarkdownRender 静态重渲染。§7.5 useStreamingDocument 项已完成。 | `src/front/chats_view.at`（消息列表 assistant 分支 + 流式 draft） |
| 022 | ~~**Phase 7.3 SecretaryMessage 留后**~~ **✅ 已修复（B 类批4）**：SecretaryMessage 移植 + SecretaryMessageWrapper（规避 composable facade ref 解包限制）；useGateInbox 完整状态机引入（registerGate/resolveGate/snooze/dismissSecretary）；forge_stream gate_reached 路由到 registerGate；forge_store ApproveGate/RejectGate handler 调真实 chats_approve/chats_reject_all API。 | `src/front/components/SecretaryMessage.vue` / `src/front/forge_store.at`（ApproveGate/RejectGate 真实 API） |
| 022 | ~~**Phase 7.3 组件 props 绑定不支持 fn 调用**~~ **✅ 已修复（auto-lang `64fb5e4d`）**：`expr_to_vue_bound_value` 加 `Expr::Call` 分支，`Component { prop: getQuestions(.msg) }` 现生成 `:prop="getQuestions(msg)"`（非 null）。a2vue golden 003_fn_call_prop 验证。auto-musk 已去掉 QuestionnaireMessage 包装组件，直接用 QuestionnaireCard + getQuestions fn 调用。 | `auto-lang crates/auto-lang/src/ui_gen/vue.rs`（expr_to_vue_bound_value Call 分支） |
| 022 | ~~**Phase 7.3 composable 声明自动调用**~~ **✅ 已修复（auto-lang `64fb5e4d`）**：use-block 语法扩展支持可选参数 `composable: useX(.arg) from "..."`。ExtImport 加 `call_args` 字段 + parser 解析 `(args)` + vue.rs 生成 `const x = useX(arg)`（空 args 仍 `useX()`，向后兼容无参 store composable）。单元测试 test_composable_with_args 验证带参 + 无参默认。auto-musk 的 useStreamingDocument 仍走 `fn:` 声明（逃生舱组件内部调用，无需 widget 层 composable）。 | `auto-lang crates/auto-lang/src/{ast/ui.rs,parser.rs,ui_gen/vue.rs}` |
| 022 | ~~**Phase 7c user 消息 mention 高亮未做**~~ **✅ 已修复（Phase 7.4）**：消息列表 user 分支现用 `UserMessage.vue` 逃生舱（v-html=renderMentions 高亮 @agent），不再走纯文本 `text .msg.content`。renderMentions helper 在 forge_helpers.ts。 | `src/front/components/UserMessage.vue` / `src/front/chats_view.at`（user 分支） |
| 022 | ~~**Phase 7c 逃生舱 ext 复制需显式声明**~~ **✅ 大部分缓解（Plan 028）**：Block 组相关显式声明已全部删除（forge_helpers/questionnaire/relay 改 .at fn 模块、StreamingRenderer/PrismCodeBlock/useStreamingDocument 改 platform 挂载自动复制、mention 由消费组件自带导入）。机制限制本身仍在：ext 复制只看 use 块，不递归 TS 内部相对 import——mention_helpers 等剩余 TS 的内部依赖仍需注意。 | `src/front/chats_view.at`（use 块）/ `auto-lang ui_gen` ext 复制机制 |
| 023 | ~~**StreamingRenderer 保留永久逃生舱**~~ **✅ 升格平台实现（Plan 028 T18/P1）**：不再以逃生舱形态被 ext 复制——.at 侧改协议声明 `component: Markdown from "platform:markdown"`，auto-man 注册表把 StreamingRenderer.vue + PrismCodeBlock.vue + useStreamingDocument 挂载到 gen `src/platform/`（相对导入改写 @/ext）。实现零改动，从「逃生舱」升格为「平台协议的 Vue 后端实现」；Rust/VM 后端未来可提供等价实现（comrak 等）。 | `src/front/components/StreamingRenderer.vue`（实现）/ `gen/front/vue/src/platform/markdown.vue`（挂载产物） |
| 023 | **逃生舱原生化行为差异**（Plan 023 P3 批量迁移 20 个组件，scoped 样式转全局 + 能力边界导致的轻微行为差异，均登记）：① RelayRunBox 日志自动滚底舍弃（template ref + nextTick watch 超能力边界，面板仍可手动滚动）；② SettingsMenu/SessionInfo/MentionDropdown 点击外部关闭舍弃（template ref 无能力表达，改按钮再次点击关闭）；③ `<transition>` fade 动画舍弃；④ GateCard approve 按钮自动聚焦舍弃；⑤ MentionInput mention 插入用 input 时捕获的 selectionStart（无 template ref 实时读取，中段插入场景有差异）；⑥ SessionInfo `code` 标签 → div 映射（视觉一致，语义元素丢失）；⑦ WikiNav 上传失败也会触发父树刷新（drop handler 命名 uploaded auto-emit，父刷新幂等）；⑧ SecretaryMessage review-in-specs 事件名连字符改 camelCase（父未接线该事件，无功能影响）。 | 各组件 .at（见 023-handoff.md §8 清单）/ 023-view-fn-component-codegen.md §3.6 |
| 022 | **`auto run` 后端生成绕过 a2r 核心**：`auto run`/`auto build` 的 `api.at → api.rs` 走 `auto-man/api_gen.rs:generate_api_rs`（手写 CRUD 模板），不调 `trans/rust.rs` a2r 转译器（仅 `db.at → db.rs` 经 `transpile_db_to_rs` 调用）。实测 `auth_login`：a2r 产物完整保留函数体 + use.rust；api_gen 产物坍缩成 `Vec<AuthUser>` CRUD。导致 `auto run` 无法一键拉起 auto-musk 全栈（前端 ✅ 后端 ❌）。解法在 auto-lang 侧：让 `generate_rust_server` 对 api.at 也走 `transpile_rust`。详见 plan 022 §8 D 类。 | `auto-lang crates/auto-man/src/api_gen.rs:1081`（generate_api_rs）/ `:301`（transpile_db_to_rs 唯一调 a2r 处）|

| 028 | **Block 组 Auto 化缺口 G1–G15 已全部解决**（F1–F9 特性落地 auto-lang：字典字面量/多参 if 条件/宿主 API/Regex 子集/Index v-model/withDefaults/v-html/SSE/HTTP/平台 markdown 协议）。**新残留**：① mention 域（renderMentions 族回调式 regex replace，F4 可移植子集外）留 mention_helpers.ts，归 G-对话壳/输入组立项；② a2ts 表达式优先级系统性问题（Bina 平铺丢括号、`/` 注入 Math.trunc 破坏浮点公式）已在 musk .at 源全部规避（中间变量），auto-lang 侧待立项；③ VM 渲染目标对 `store` facade（vue 合成的 `const store = reactive(...)`）报 Undefined variable 警告 + TS ext 依赖（injectStyles）显式 link 错误——VM store facade 概念缺失，归 VM 渲染目标立项。详见 [docs/plans/028-block-autolang-full-migration.md](028-block-autolang-full-migration.md)。 | `src/front/*.at` / `tmp/plan028-parity/`（148 项对拍） |
| 059-FU1 | **✅ 已核销（2026-09-04，auto-lang PLAN-536 交付，重测实证）**——三题根修落地并合并：①timer 失效广播（handler 执行中崩置 dirty+热重载拍脏桥接 500ms 兜底）；②子件 Init 挂载语义（实机每实例每子件 1 次，对比本行原记 5498→16293）；③时间标签（Date.format 秒/毫秒 1e11 双口径归一+musk 侧去 ×1000 i32 回绕，重测实测 19:54:10==库中 timestamp 本地时）。同批交付：DeleteConfirmDialog 端口链（delete_confirm.at→vm.at/web.at）+会话卡 × 兄弟悬浮（top-2+z-10）+T9 模态弹出实机截图（autoui-screenshot-1788515730934）。**收尾余项**：× 水平位置落左上（渲染旁路，auto-lang T9 收尾）；内联确认行退役留 musk 侧 T9 跟进（模态已可用，确认行并存不阻塞）。原：**VM 轨 timer 驱动的状态回写不触发消息列表重渲染（用户实机反馈 2026-09-03 晚，实证非 musk 侧逻辑缺陷）**：发送消息后 `PollStream`（500ms 轮询 chats_get_session 全量回填）实跑 18 拍且完成启发式翻转（末条 assistant 非空才置 false ⇒ refetch 成功、store 已含回复），但聊天画布永不刷新——用户视角"AI 没回复也没有等待指示"（VM 无 SSE，`streaming` 等待指示依赖 PollStream 期间 current_draft 为空的分支，而该分支只在首拍前可见）。后端 6 秒即回复且落盘（tmp/musk-demo conversations 9e603d92 turn seq3+tool_call+tool_result）。变通：重选会话（SelectSession 为 handler 驱动，重渲染正常）即见回复。同场观察：①子件 Init handler 单会话期被调 5498 次（重渲染循环反复跑 Init，首实例还见 LoadSessionList 随之重复——Init 副作用 per-render 重入）；②消息气泡时间标签 08:34 而 turns 实际 19:54（msgTimeLabel 时区/换算异常）。根修归 auto-lang：timer/外部事件路径的视图失效 + Init 重入语义，建议随 VM 轨反应性专项立项。 | `src/front/forge_store.at`（timer PollStream）/ auto-lang VM runtime（视图失效/Init 重入）/ `src/front/forge_helpers.at`（msgTimeLabel） |
| 059-T9 | **✅ 消费回归完成（2026-09-04，PLAN-059 T9 实机三场景全通）**——删除确认 alert-dialog 模态浮层+取消/确认派发全链路（子→父载荷直达、session_list 3→2 真删除）、workspace popover 自管嵌套开合、settings dialog 居中模态/显式关闭，截图 p059-musk-scene{1,2,3}-*.png 入 attachments；VM 内联确认行与 .session-delete-strip 抑制退役（核销 059-FU1 余项）。**联测期 auto-lang 双根修已折入 master（3837aa8b5）**：① a2ts Date.format ts 发射括号失衡（536 题3 回归，`return (new Date(G)).toLocaleTimeString(...));` TS1005，d848cb29a）；② 子→父声明式路由零参父 handler 帧错位（dispatch_parent_route 恒塞 Nil 载荷 → `[self,Nil]` 帧 → CancelDelete 崩 Invalid object ID: 0，即 536 T9 留白的取消派发终验，aa92a821e，探针 plan059_child_emit）。**T9 实机收尾追加（2026-09-04 晚）**：①会话卡 × 三连败定案回归 hover 标题行形态（兄弟浮 × 几何落左上/常显/抢卡片点击，dbe52a7）；②工具卡/思考块展开回归 ForgeStore 单槽字符串通道（消息对象直写为循环副本写读侧不更新，c13c250）。**余项（归 auto-lang 二期家族）**：⓪ **fixed_both 方钮内 Image 图标不居中**（头部 + 钮 icon 落左上,同钮 text 内容居中正常;renderer fixed_both 居中臂对 Image 内容未生效,div 包裹实验无效——renderer.rs:3372 臂 + Image 臂 4474 container 返回，实机截图 tmp/header-zoom.png）；① 解释器轨家族根（alert-dialog/dialog/dropdown-menu）**需 trigger 锚件在场**才绘浮层（anchor=Empty 时面板退化流内不绘——实机对照实证；空 trigger 即变通）；② **受控 open（显式绑定自管）形态在 VM 无 ESC/外点自动关闭**（minted dismiss 仅解析器铸造形态与 popover 注册表形态接管）——settings dialog VM 关闭靠显式钮，web 走 reka 原生；dropdown ESC 不关同族（T5 残余）；③ VM Modal 暗幕 scrim 实机截图未见明显变暗（暗色主题下目验歧义，待专项目验）；④ 弹层 chrome 固定档（dialog w-96/dropdown w-44/popover w-72），无宽度 prop 透传。 | auto-lang 3837aa8b5（dynamic.rs/child_emit/ts_adapter）/ `src/front/settings_menu.at` / `src/front/workspace_selector.at` / `src/front/chats_view.at` |

## 📋 未来增强

| Plan | 描述 | 参考 |
|---|---|---|
| 018 | **休眠镜像 full parity**：`tools.rs`/`spec_tools`/`orch_tools`/`server_serve` 等 ag 镜像为简化 dormant（description/schema 文本与 hw 有差异 + execute 依赖 extern stub）。计划 §10.6 Phase 4 评估 + §13 C 类已文档化为"设计内的等价镜像，非缺陷"。full parity 需 `.view` 手术 + 元数据对齐，留待后续接线计划。**注：Plan 019 已完成 `server_stream` 接线；Plan 020 Phase G/H 已完成 `relay_driver`(drive_run/loop/step 核心循环)+ `server_serve`(drive_* 已搬出,仅余 serve/settings_link 休眠骨架)的 full parity 接线——这两个模块不再属休眠镜像,现仅 `tools`/`spec_tools`/`orch_tools`/`server_serve` 休眠。Plan 021 §8 评估后明确这 4 镜像留待未来:激活成本极高(本地 trait Tool 不兼容 auto_ai_agent::Tool / orch_tools 缺 2 工具 / descriptions 缩水 / 需跨 auto-lang 改转译器)、收益为零(生产 agent 硬编码 hw tools 无切换路径)。** | `src/auto_generated/{tools,spec_tools,orch_tools,server_serve}.rs` |
| 018 | **HTTP 层测试缺口**（§13 E1）：`/api/run*`、`/api/chats/.../stream`、`/api/conversations/.../stream` 无 HTTP 层测试。**注：Plan 020 Phase D/E 已补 `/api/forge/*` + `/api/settings-link` 8 项；Plan 021 已补 `/api/files/*`(parity_files 5)+ workspace 端点(parity_workspace_endpoints 4:chats DELETE/drift/rebuild/related)+ config 端点(parity_config_endpoints 3:roles/app-config/harness,AUTOOS_HOME 隔离)。剩余 `/api/run*`/chats stream/conversations stream 仍无 HTTP 覆盖(plan 019 流式范围,逻辑层已覆盖)。** | — |
| 391 | **多段路径 codegen**：`std::env::var("X")`（多段 `::`）parser 现可解析，但 codegen 对小写模块段（`env`）仍发点（`std.env.var`）。单段 `env::var` + `use.rust std::env` 可用。多段需改 rust.rs:3097-3121 让小写段也认 `::`。 | `auto-lang trans/rust.rs:3097` |
| 029 | **D 组永久保留(架构性,非债务)**:① components/StreamingRenderer.vue + PrismCodeBlock.vue + composables/useStreamingDocument.ts——platform:markdown/代码块的 Vue 平台实现;② setup_auth_fetch.ts——App 引导 window.fetch monkey-patch(jwt+workspace 注入);③ composables/useT.ts——vue-i18n 宿主库桥(t() 工厂 + locale 读写);④ raw_upload.ts 瘦身版——XHR FormData 进度上传 + .text() 加载(Http.* 只展开 .json())。 | src/front/ 各文件 |
| 029 | **跨 store 接线桥(v1 单 store per file 限制)**:gate_router.ts(forge.current_gate/relay.gate_signal watch → GateInbox Register/Resolve)+ relay_command_runner.ts(RelayStore StartRun/AdvanceRun + Forge 推消息)——纯接线无状态,多 store 放开后可并入 .at。 | src/front/composables/ |
| 029 | **codegen 缺口(登记待修)**:① shadcn Button 映射丢弃动态 class/title 绑定(settings_menu 色板,已用容器子选择器 + :has(svg) 绕开);② store 内联模块 fn 的参数名若与 state 同名,生成 .value(字符串参数变 undefined——visual_store 参数改名规避,系统性风险在 ts_adapter state-ref 语境);③ fn 模块 try 内嵌 if 里的 return 仍不支持(parser 边界,Plan 022 已知);④ view 的 for-range 不支持直接调 fn(经 computed 中转)。 | auto-lang ui_gen |
| 029 | **迁移行为差异(均登记)**:① auto 主题的系统偏好 matchMedia change listener 不可表达,仅 Init/SetMode 读一次;② session copy 的 2s setTimeout 自动复位不可表达,✓ 保持至下次复制;③ 全局快捷键挂视图根 onkeydown(非 document 级)——目标元素均在应用内,等价。 | visual_store.at / session_info.at / app.at |
| 050 | **VM/Vue 渲染一致性诊断规程——双轨排障必查 web-only.ts 差异源**：凡发现 VM(iced) 与 Vue 渲染不一致,除组件 style 渲染能力缺口（PLAN-050 C1-C7：内容子树按钮对齐/单侧边框/slot/透明度色/图标桥/浮层/文本插值）外,**必查 inject_styles.web-only.ts 是否引入差异**——留守项=字体(Noto vs VM Inter,CJK 回退缺失)/主题变量覆盖(.dark"原版紫"暗色板 vs theme.rs shadcn 默认值,secondary/border/muted/accent 存在微差)/滚动条/链接色/zebra/@autodown 第三方覆盖。残留清单与逐条迁移矩阵=docs/designs/010（A 组已内联,B1 mention 上下文已内联,留守理由在案）;slot 替换+颜色继承需求说明=docs/designs/009（携 auto-lang 立项）。另：VM 按钮→文字颜色继承失效,文字显式声明色才可靠（Plan 409 §8 模拟不完整,像素取证 OnPrimary 泄漏）。PLAN-050 执行完毕(2026-08-30)：T1-T12 全勾——C1 内容子树 flex 行方向+C5 图标桥(builder 组件臂+进程级注册表+renderer 27 glyph)/C6 面板降级类串+login 掩码+onenter/C7 三断链修复(decode_tagged_nv null 臂/AWAIT_FUTURE 保标签/t()+i18n_lookup 查表)均折入 auto-lang master;双焦点/autoui_type 归因由上游 Plan 483 清偿(011 需求书)。T11 VM 四界面 4/4(t11-checklist.md);浏览器侧对拍与三门禁复跑留待 051 折 master 后(主检出 chats_view.at 点分语法需 051 解析器,装机会话期临时不匹配)。**门禁复跑全绿(2026-08-30,051/484 折后 master 6f7189bce)**:build strict ✓/vitest 23+1skip ✓/phase1-leaves 30/30 ✓/style-parity 12=基线差分恒等 ✓/vm-probe PASS 61162B ✓/first-run alive reds=0 ✓;musk plan-050-dev worktree 已折叠清理,auto-lang auto-musk-dev 留置(并行会话在用)。gate 卡 ${titleText} 裸串族=后续批次。**Phase 2 五项修毕(2026-08-30)**:#1 items-baseline 降级 ItemsStart→ItemsCenter(v0.1.0 纵向居中)/#2 nav-item 居中双修(并行 plan414 高度臂让位显式对齐类+b5aaf7f54 已折 master 含实机截图;preset justify-start+NavItem.vue 资产同步)/#3 mt-auto→Column 前置 Fill 弹性占位(匹配 master 通用 MarginTop(SizeValue::Auto) 形态,专用变体去重+决策函数回归钉)/#4/#5 musk 降级类串(98006de)。落点 auto-lang master **318d011a8**(plan-050-p2 折叠)+release 重装;门禁:plan050_ 13 绿/--lib 差分零新增(基线 8-9 败 dock/notif/settings/stage3 flaky 族持平)/vm-probe PASS 61419B/first-run alive reds=0;#1/#3 rail 像素目验留用户(图像通道限制惯例)。 | PLAN-050 / docs/designs/009 / docs/designs/010 / src/front/inject_styles.web-only.ts |
| 051 | **VM 会话主界面闭环移交清单(2026-08-30 execution_done)**:**✅ 七能力全交付+会话闭环验收**——C1 Enter 键盘接线(修饰规整 onkeydown.enter.exact.prevent+MCP 焦点声明派发)/C2 子→父 msg 参数回调通用路由(child_emit 双表+前置快照语义)/C3 computed for 源回退(resolve_iterable→eval_computed+call_vm_fn)/C4 adapter widget 注册+use.web 点分 kind(use.web.fn/component/composable,显式 fn 校验 typo 报错)/C5 流式轮询降级(forge_store PollStream timer 500ms when:.streaming,全量回填+末条非空 assistant 停拍)/C7 timer 声明块 DSL 全栈(widget/store,AST/parser/vue/VM 订阅+fire_timer 门控,017 demo 双轨)/MCP 归因对位双修(find_view_by_path 同构化+styled_vtree 同源)。**实机验收**:登录→选会话→发送(Enter/按钮)→乐观 push 即时入列→轮询回填 2s 跟随后端(决定性证明)→气泡分向/徽章/hairline 渲染(T8 多模态+vtree 双证)。**上游留档(降级登记,归后续计划)**:①SSE 无 VM 形态阶段2 桥泛化后 PollStream 退役(KD-047 G1 路线);②VM for-in 直接调用源栈失衡(待澄清6,musk 已 let 绑定绕开);③Date.format 富格式无 VM native(时间戳空白,Date.now 已补);④i18n 参数模板未插值({count}/${runId});⑤call_vm_fn retain 未配对释放(per-frame 生命周期,长会话内存增长);⑥markdown 纯文本降级(renderer.vm.at,comrak 富文本上游项)。**基线红登记(051 零新增,差分实证)**:style-parity 12 条 border-t/b(master 先在,T7 差分同败);auto-lang --lib 5 败(plan050×2/dock×2=473 在途债/code_editor)。**常驻门禁**:build strict/vitest 46+2skip/phase1-leaves 30/30/vm-probe PASS 60966B/first-run alive reds=0/style-parity 基线 12(差分恒等)。**环境注记**:vendor/@autodown/engine dist 为本地产物(gitignored),新 worktree 需自主检出复制;demo 后端无 aaid daemon,assistant 增量流不可现(轮询机制经 API 注入决定性证明)。 | [PLAN-051](051-vm-chat-roundtrip.md) / auto-lang plan051_timer_tests+tests_plan483_d4 / scripts vm-smoke.mjs(017 五断言) | | **Phase 2 附记(2026-08-30)**:会话壳视觉五缺陷修毕——①图标注册表第三路(子模块顶层 use.web component)②列表删除钮 hidden(VM 不渲染 hover 件,web CSS 特异性零回归)③搜索框 min-w/w④容器 min_h/min_w 消费⑤往返实测;附带 i18n {count} 插值实机生效。**新债**:MentionInput .cancel 声明式路由未达(子 handler 派发而宿主 .StopStream 未接上——**composer 锁死主线已由 PLAN-055 ⑧ 解除**(2026-09-02:.Send run=true 显式触发+PollStream 收尾,streaming 随回复落定);.cancel 无参路由缺口本身仍在,PLAN-055 T6 登记);容器 min-h 语义近似 Fixed 下限(非弹性最小高);~~VM 删除会话在 VM 轨不可达~~ **✅ 已修复(2026-09-02,PLAN-055 T16)**——删除钮类串去 hidden 改常显(VM 无 hover 态,观感偏差登记),onclick.stop 点击实删后端实证;judge/Read 图像通道在本环境仅 CDN 化无法目检(像素级终验靠用户)。上游 auto-musk-dev@2c36322c0 待合。
| 493 | 【2026-09-05 PLAN-536 T12 复验更新：**①发送链崩已收敛**——实机 E2E 多轮（auto-lang plan-536-dev 二进制 + musk plan-536-musk-dev）发送→SendInput→store.Send/store.StartStream 全链零崩（Invalid object ID 未再复发；组件层探针 plan536_send_chain 锁定一参回调链契约），归因 aa92a821e dispatch_parent_route 帧对齐根修覆盖（该修复落在 493① 立案之后，此前从未复验）。**②同批新证**：Sse.open(.OnStreamEvent) handler-as-value 实参在 VM 抛 Field not found（KD-047/055-4② 未解）会经 StartStream 尾部抛点杀死 PollStream 门控——musk 侧已以四修绕行（forge_store.at 8b1ae23：streaming 置位前移/头部直连 close/启发式回合增长守卫/when 门摘除+deadman 2 分钟窗），PollStream 兜底链恢复；**残余最后一里=画布投影**（KD-057④ computed 读侧 + 新立案 auto-lang KD P536-D2 跨模块 SET_FIELD 不可达根态，state-scope 专项）。**原条目：**mention-native textarea 落地(2026-08-30,上游 auto-lang Plan 493)**:composer mention 高亮升为 textarea 能力声明(`mentions:`+`mention_class:`),.at 删 backdrop 叠加结构;Vue 由 codegen 发射兄弟对(类串自 textarea 推导),VM 走原生 Highlighter(名单 state-rooted 受限解析,不进 call 链)。**新发现先在债(A/B 双仓 master 复现,非 493 回归)**:①composer `.send` handler 链垃圾引用崩溃——`Invalid object ID 18446744071562067969`(child handler_MentionInput_send)或 `unknown_obj:4000041 无 .push`(parent handler_ChatsView_SendInput)两形态抖动出现,Enter/按钮路径消息均入列但 **draft 不清空**(051 期按钮路径曾实证清空,当前两仓 master 均崩——期间 auto-lang 有新提交介入,定位归上游 VM 债);②VM 实例静默退出(KD-048-a)本session 观测加剧(部分实例 <30s),登录链偶发 token 不落,活体验证受阻;③composer VM 单行观感=text_editor 缺省 Fixed(30)(h-full/min-h CSS-only,Plan 053 注记)——已补显式 `height: 72`(vue 轨不消费,上游已纳 attr 过滤)。**待复验**:@mention 蓝色着色活体像素终验(机制链 T3/T4 单测全绿;复验手段=AUTO_DEBUG_MENTIONS=1 起实例看 `[493-MENTIONS]` 段产出+窗口像素)。**环境注记**:vendor/@autodown/engine dist gitignored,新 worktree 需自主复制(同 051 注记);@autodown 包名漂移 junction 桥接会话级依旧。 | [auto-lang Plan 493](../../auto-lang/docs/plans/493-mention-native-textarea.md) / mention_input.at / 上游 aura_view_builder mention_segments+resolve_mention_names |
| — | **VM/TS 语义等价性缺陷族①–⑤ 已修（PLAN-057 两仓联合根修，2026-09-03 execution_done）**：①SET_FIELD 新键插入——ObjectData 臂末 else 改产出 Str 新键（obj.set=insert，JS 赋值语义；Int/Bool 键格式与 GenericInstanceData 分支保持 Plan 118 报错）——a1 全家福空白直接死因根除；②for-in Call 源通道泛化——E5b 索引通道承接全部 Call 源，例外=迭代器协议链（.iter/.take/.skip/.rev/.chain/.zip+**生成器 fn**）与 stream/sse_ 惰性流（生成器例外为 T9 门禁回归修复：generator_tests 5 红→绿）；③web 内建 natives 补齐——Array.isArray（id 1919，encode_bool 推栈）/JSON.stringify（1918，固 arity 3+codegen 缺参补位+ObjectData 序列化臂+键字典序）/JSON.parse 数组形态（shim_str_len 堆句柄判定扩 object/list-tag+ListData 全四变体）/Math.trunc（int 恒等防回绕）/Math.imul（i32 回绕乘）——后两件为 census 漏计项；另 CALL_SPEC 三处未知方法兜底配平（str/List/unknown 接收者臂原只压 None 不弹 receiver+实参→栈失衡平移参数槽）⑤标签乱码根修；is_static_method 两处白名单补 JS 全局名；**编译期门禁**：模块式 web 内建全局调用（接收者字面∈Math/JSON/Object/Array 且非变量）解析失败升编译错误，豁免=调用点 `// vm-safe-allow`（Codegen.source_text 注入）；④for-in 裸字符接收者——PLAN-055 Char 恒等臂从 CALL_SPEC 链尾提升到 `<unknown:` 接收者臂之前（原被整型字面量方法族臂吞 None）。**T11 实机补遗两件**：嵌套 compound 字段 stringify 裸句柄修复（nv_to_vm_value 堆判定前移——GET_FIELD ObjectData 臂嵌套 VmRef 以裸 int 句柄 ≥HEAP_ID_BASE 出栈，i32 臂先吞序列化成数字，工具卡 Arguments 显示 "4067504" 现场）；Block 卡片框架双轨注记（原始 CSS 为 Vue 专属直通，六类卡根补 tailwind 工具类，沿 ThinkBlock 头混串先例）。**回归面**：tmp/vmprobe/case_{setfield_newkey,forin_call,web_builtins,str_charcode}.at 四件全绿（4/5/8/3）+auto-lang musk_vm_track_p057 全族 28 用例+vm-first-run alive reds=0+musk cargo 617 绿+vitest 23+1skip。**musk 侧配套**：scripts/vm-safe-lint.mjs 五模式静态门禁（白名单随 T6 增补，现零红+3 豁免）；055 T0 规避解除（toolArgsJson 直通/问卷 isArray 判别恢复，messageBlocks 字面量重建按待澄清③保留）。**缓期项（未修，转 057-2 行）**：markdown/autodown 富渲染（计划明示 out of scope，等 autodown 下一波）+读侧族/int 回绕/Regex 族见新行。 | auto-lang master 016e95df7+6574a319a(cb6e8a6ef) / [PLAN-057](057-vm-ts-equiv-block-parity.md) / tmp/vmprobe/case_*.at |
| 055 | **VM/Vue 聊天对拍第三批(2026-09-01)遗留上游债(均已绕开/降级,功能不受阻)**:①VM float 管线缺陷——fn 模块内 `int × float` 产出未打标签的位型垃圾(15.3f32 位型以 Int 形态出现)、`Math.round(4.25)` 恒 0、regex native 返回未打标签字符串索引(实机 "203200");musk estimateTokens 已改整数定点+索引式 char_code_at 绕开,气泡文案取值回落 content state,根修归 auto-lang VM 值打标体系。②VM `.length`/`.len()`/ARRAY_LEN(str)/char(码点)接收者 char_code_at 已修为 JS 字符语义(auto-lang c964ffa81),但 for-in 循环变量上直接调方法仍落 Nil(P055-M 实测,绕开中)。③视图级 `if .computed` 真值判定已修(aura_view_builder 裸点臂补 computed 表兜底+true/false 字面量)——此前 chat_message 用户分支/hasTime/hasError 全体恒假。④div{html:}(v-html) VM 轨=去标签纯文本降级(mention 高亮 web-only);⑤ThinkBlock 展开区——**PLAN-055 T12 后（2026-09-02）pre/code 进容器转换臂**：padding/border-t/圆角框/淡底实机已渲染（截图在案），max-h 尽力 clip、滚动仍 web-only;⑥max-w-[85%] 百分比 VM 不支持(长用户消息全宽,不影响抱合);⑦时间戳——**PLAN-055 T7 收敛（2026-09-02）**：gen 轨 Invalid Date 已修（乐观/新消息写 created_at 秒+量级自适应守卫），VM 制式根因钉死=int 乘法 32 位回绕（值偏移非 Invalid），根修归 PLAN-057;⑧vue-tsc 对 a2ts range( 缺 helper 已修(ui_gen vue.rs fn 模块检测注入)。 | auto-lang c964ffa81+eda3a5a5e / musk auto-musk-dev-1 d594146 / P055 探针族(musk_vm_track_tests p055_*) |
| 056 | **specs_detail.at TestDetail fixture pre 首行模板缩进伪影（PLAN-056 复审在案延后）**：与聊天折叠块 pre→div 同根因（codegen 对 pre 子树保留模板换行+缩进为字面内容，Vue isPre 语义），位于规范页不在聊天块型计划范围；修复模式已实证=pre→div+既有 whitespace-pre-wrap 类承接（056 T3-T5），待规范页批次收口 | [PLAN-056](archived/056-chat-block-styles.md) 详细设计 T3-T5 / specs_detail.at |
| 056 | **gen/front/vue/dist 无内容 hash 且响应无 Cache-Control——部署缓存债（PLAN-056 T6 复审发现；main 先在，非本计划引入）**：重新部署后浏览器启发式缓存吐旧 JS（T6 复审实机踩到，强刷才可见）；修法方向=vite build 产物 hash 化或静态服务对 index.html 加 no-cache | [PLAN-056](archived/056-chat-block-styles.md) T6/T7 复审记录 / gen/front/vue vite 配置 |
| 055-4 | **VM/Vue 聊天对拍第四批（PLAN-055，2026-09-02 execution_done）残项与新债**：**✅ 已收敛**——⑧AI 无回复（后端 run=true 显式触发+per-session 守卫+PollStream 收尾，E2E 实机问答三对在案）／①导航高亮（复测现行 master 无断链，双用例钉死+双视图实机截图）／②会话列表（tooltip/Info 删、VM 常显红钮实删）／③空闲纸飞机（实机目验）／⑤重试钮三轨全删（web 冻结豁免记录）／⑥gen 搜索（$event 包修+实机精确过滤）／⑦gen Invalid Date（created_at 秒+守卫）／④样式面（pre/code 容器臂：留白/分隔线/圆角框实机）。**残项（等价性缺陷族读侧，归 PLAN-057）**：⑥VM 过滤投影恒 0（computed 内 VmRef 域读取恒空——函数层同形探针全绿，断点在 computed 求值上下文）；④VM ThinkBlock 隔离读侧（写侧键列表全链通；chevron computed 读共享末值 ▼▼ vs 视图 if 臂恒真双内容——子件 if/computed 求值分歧）。**新债登记**：①VM 按钮发送 handler_MentionInput_send 非确定崩（4/7 次 Invalid object ID 哨兵形态，一次成功完成 E2E；493 行①同族加剧样本）；②StartStream 的 Sse.open(.OnStreamEvent) 实参解析在 VM 抛 Field not found（KD-047 SSE 桥族，乐观 push/发送在崩溃点前已生效不阻塞 run=true 主线）；③VM 内部 split-mode 服务绑 0.0.0.0:8080 在 Windows 保留端口段（8068-8167）必败——冷启动偶发致命（等待超时退出 1），规避=启动加 `-B <其他端口>` 或 AUTO_HTTP_PORT；④KD-048-a VM 静默退出本批频发（多实例 2-3 分钟 exit 1，无 panic——与 048 行 OPEN-a 同案，加剧样本在案）；⑤musk gen 重生成偶发陈旧缓存——删组件/改 use 行后需 `touch` 源文件强制刷新（ChatMessage.vue 陈旧实例在案）；⑥Square 流式停止钮实拍未获（进入流式态被①的 .send 不稳定拦截；代码臂+同管线佐证在案）；⑦VM 常显删除钮 vs gen hover 显隐观感偏差（选型 D3 已知，iced 无 hover）。 | [PLAN-055](055-vm-vue-chat-parity-batch4.md) / tmp/vmprobe/{t1_datefmt,t3_filter,t4_idx_assign}.at / auto-lang auto-musk-dev(bc3cccabb+1b6da51ee) |
| 057 | **PLAN-057 T11 实机对拍残差（2026-09-03，VM vs gen 双轨矩阵）**：①**__json_object 字符串字段读污染（新发现，pre-existing——b68ce46fb 基点二进制复现=非 057 回归）**：JSON.parse 产物的字符串字段读取返回类型名 "str"（wl_probe21：j.kind→"str"、字面量 {type:"x"}.type→空而 .kind 正常）——问卷卡 VM 渲染的最后堵点（questionnaireFor 的 json.type 判定失效）；int 字段读正常（po.answer=42）。②**Regex.match 恒空**（055-3 ①族实证加深：围栏提取形态 0 匹配，wl_probe18）——musk 侧已绕（questionnaireFor/stripQuestionnaire 改 indexOf 纯串通道，11b6c20），auto-lang regex native 根修仍欠。③ThinkBlock 展开读侧族（chevron computed 读共享末值——写侧 think_open 键全链通实机验证 ["bsmsg-a1"]，055-4 已登记）。④卡片框色差近似（report 绿边→border-border、questionnaire primary 系近似）+hover 态缺失（VM 无 hover，055-4 已知选型）。⑤ext-stub 警告 54 项口径：44 项图标组件为图标桥假警报（lucide 字形实渲染，视觉证据在案）+10 项 web 专属平台函数（vsSetView/upload 族，VM 无此概念，登记豁免）。⑥**VM 实机合成输入被 pixel-identity 守卫拦截**（CUA click/scroll frame-identity mismatch）→视口下方块（Run 窗口/报告卡/问卷）与展开态的自动化验证受限，快照视口裁剪（autoui_vtree 同裁），终验靠用户实机（用户已实测点击展开+滚动并反馈）。⑦markdown 富文本纯文本降级（计划缓期项）。 | tmp/vmprobe/wl_probe21.at / [PLAN-057](057-vm-ts-equiv-block-parity.md) T11 / docs/plans/attachments/p057-t11-vm-*.png |
