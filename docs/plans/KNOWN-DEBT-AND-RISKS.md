# Known Debt & Risks

> 复审（plan-archiver Step 2.5）登记的 workaround / 一致性遗漏 / 已知限制 / 未来增强。
> 每行一项，按严重度分级。新增条目请在对应分级表追加。
>
> **a2r 转译器限制统一登记在 auto-lang `docs/plans/391-a2r-parity-debt-from-musk.md`**；
> Plan 391 的 D1-D6 已全部闭环并合入 auto-lang master（2026-08-06），
> auto-musk 侧对应的 .at 变通已逐项去除（D1/D2/D3/D5 + 此前的 wiki 两项）。

严重度图例：
- 🔴 **高风险** — 特定（非平凡）条件下可能引发 UB / 数据损坏
- 🟡 **一致性遗漏** — 功能正确，但代码未达计划自身的一致性目标
- 🟢 **已知限制** — 设计决策（非 bug），值得记录
- 📋 **未来增强** — 留待后续计划的优化 / 清理

---

## 🟡 一致性遗漏

_无。_

## 🟢 已知限制

| Plan | 描述 | 参考 |
|---|---|---|
| 038 | **auto-icons 渲染层降级**（T9 canary 实证 2026-08-23）：.at UI 元素集不支持 svg 语义——`svg`/`path` 经 a2vue 产物退化为 `<div :viewBox=...><div :d=.../>`（SVG 语义丢失，渲染不可用）。数据层不受影响：`src/front/lib/icons_data.at`（52 图标）已交付并入编译图（ports/icons.web.at re-export），`scripts/lib-parity/icons.mjs` 数据对拍全等。解除条件：auto-lang svg 节点能力（auto-lang Plan 442 A4 已按本 canary 结论登记）；解除后实现 Icon widget 并恢复 @vue/server-renderer renderToString 对拍。 | `src/front/lib/icon.at`（stub）/ [442 A4](../../auto-lang/docs/plans/442-cross-platform-closure.md) |
| 018 | `task_plan.at` C1：`impl TryFrom<Node>` trait impl → `static fn from_node`（Auto 无 trait impl 语法）。**a2r 计划391 D6 已让 `impl Trait for Type` 报清晰错误**（不再静默反转），但 Auto 语言层面的 trait impl 支持仍是未来设计决策。parity 分别调 hw `try_from` / ag `from_node` 比行为。 | `auto-src/task_plan.at:272` / [391 D6](../../auto-lang/docs/plans/391-a2r-parity-debt-from-musk.md) |
| 018 | ~~`app_config` 的 `AAID_URL` env 覆盖在 a2r 产物中缺失~~ **✅ 已修复（Plan 021 B1，2026-08-07）**：`app_config.at` 的 `effective_daemon_url` 现读 `env::var("AAID_URL").ok()`（与 hw file < env < default 一致；391 D4 让方法链可解析）。`parity_app_config.rs::parity_effective_daemon_url` 断言两侧行为一致。 | `auto-src/app_config.at` |
| 018 | ~~`wiki.at` TreeNode file 节点 `modified = None`~~ **✅ 已修复（Plan 021 B2，2026-08-07）**：`wiki.at` 新增 `file_modified(entry)` 辅助 fn（`modified().ok() → duration_since(UNIX_EPOCH).ok() → let secs u64 = as_secs()` 抑制 cast）；build_tree file 节点 `modified` 取真实 mtime。`parity_wiki_http` 逐键比对（含 modified）。 | `auto-src/wiki.at` |
| 018 | a2r 输出验证器对 specs.at 报 `unbalanced parentheses (depth: 1)` 警告（编译通过、测试通过，疑为字符串字面量内括号被误判）。非阻断，记为待查。 | `auto-src/specs.a2r.rs` 转译输出 |
| 019 | ~~ag 流式 handler 即时错误走 200 SSE + error 帧~~ **✅ 已修复（§6.1，2026-08-06）**：`run_stream_handler`/`workflow_run_stream` 现在在 `mpsc_channel()` 前前置 `mode_exists`/`workflow_exists` 校验（与 hw fail-fast 一致），坏 mode / 坏 workflow → 400 JSON。契约测试 `ag_run_stream_bad_mode_returns_400` + `ag_workflow_run_stream_invalid_workflow_returns_400` 锚定。 | `src/auto_generated/server_stream.rs`（前置校验）/ `extern_impl.rs`（`mode_exists`/`workflow_exists`） |
| 019 | ~~conversation_stream 的 broadcast Receiver 存 side-table，客户端断开时条目不回收（累积泄漏）~~ **✅ 已修复（§6.2，2026-08-06）**：`conversations_subscribe` 返回 `BroadcastSub`（rx 包 `Arc<Mutex<Option<Receiver>>>`），被 `conv_event_stream` owned → stream drop（客户端断开）→ Arc 归零 → rx 析构。不再用 registry 存 receiver。测试 `broadcast_sub_drop_reclaims_receiver` + `conversation_stream_drop_reclaims_receiver` 断言 receiver_count 回落。 | `src/auto_generated/extern_impl.rs` `BroadcastSub`/`conversations_subscribe` |
| 019 | mpsc channel 缓冲 64 条：run/chat/workflow 流在流建立前爆发超过 64 事件时 `try_send` 静默丢帧。**与 hw 完全等价**（hw 三 handler 同样 `channel::<Value>(64)` + `try_send`，`server.rs:367/618/904`），非缺陷、无需修复，仅登记备忘。 | `src/auto_generated/extern_impl.rs` `mpsc_channel` / `server.rs:367,618,904` |
| 020 | **a2r spec trait 无 supertrait 语法（Send+Sync）**：`TaskPlanExecutor` 等 spec trait 无法声明 `Send + Sync` bound → 生成的 `execute` future 非 Send,`relay_task_plan_start` 无法用 `tokio::spawn`,改用独立线程 + current-thread runtime `block_on`(future 全程不跨线程)。行为与 hw `tokio::spawn` 等价(后台执行 + HTTP 立即返回),非功能阻塞。**根因在 auto-lang 侧(spec 语法 + codegen 决策,类比 391 D6),完全未在 auto-lang 登记** —— 待 auto-lang follow-up 立项解除;解除后可把 spawn 路径改回 `tokio::spawn`。 | `auto_generated/extern_impl.rs` `relay_task_plan_start`(独立线程 block_on)/ `auto_generated/task_plan_engine.rs` `TaskPlanExecutor` spec |
| 020 | **a2r 借用版本漂移(task_plan_engine 既有块)**：Plan 020 Phase H 期间发现,当前 auto-lang master 的 a2r 对 `self.field`(`String`)在 extern 调用点发射 `self.field.clone()`(owned),而之前提交的产物发射 `&self.field`(借用);`@T` 实参的 `&` 注入也有差异(`&&handoffs` vs `&handoffs`)。这导致 task_plan_engine.at **既有**块 re-transpile 与 `auto_generated/task_plan_engine.rs` 有 drift(编译/测试仍绿,仅 codegen 风格差异)。Phase H 新增的 `RelayTaskPlanExecutor` 块与既有无关、零 drift。**非阻断**;待 a2r 借用启发式稳定后,统一 re-transpile 全模块消除 drift。 | `auto-src/task_plan_engine.at` / `auto_generated/task_plan_engine.rs` |
| 022 | ~~**Phase 7c RelayCard 为状态展示版**~~ **✅ 已修复（B 类批3）**：`RelayRunBox.vue` 替代 RelayCard 状态展示版，完整交互（subscribeToRun 实时日志 SSE + resolveGate 审批）；useRelay/useEventRouter/useGateInbox 逃生舱引入；`/relay`//`/superpower`//`/spec1` 命令路由（relay_commands.ts）。 | `src/front/components/RelayRunBox.vue` / `src/front/composables/useRelay.ts` |
| 022 | ~~**Phase 7c professionOptions 用内置默认列表**~~ **✅ 已修复（B 类批1）**：useAgentConfigs 逃生舱调 `/api/forge/relay/professions`（替代空 stub），MentionInput 优先用动态列表（DEFAULT_PROFESSIONS 仅作 fallback）。 | `src/front/composables/useAgentConfigs.ts` |
| 022 | ~~**Phase 7c 流式 draft 走静态 markdown 渲染**~~ **✅ 已修复（Phase 7.3 续）**：流式 draft + assistant 消息现均用 `StreamingRenderer`（逃生舱整体引入 useStreamingDocument 增量 JSON 解析 + StreamingTable + markstream-vue），不再走 MarkdownRender 静态重渲染。§7.5 useStreamingDocument 项已完成。 | `src/front/chats_view.at`（消息列表 assistant 分支 + 流式 draft） |
| 022 | ~~**Phase 7.3 SecretaryMessage 留后**~~ **✅ 已修复（B 类批4）**：SecretaryMessage 移植 + SecretaryMessageWrapper（规避 composable facade ref 解包限制）；useGateInbox 完整状态机引入（registerGate/resolveGate/snooze/dismissSecretary）；forge_stream gate_reached 路由到 registerGate；forge_store ApproveGate/RejectGate handler 调真实 chats_approve/chats_reject_all API。 | `src/front/components/SecretaryMessage.vue` / `src/front/forge_store.at`（ApproveGate/RejectGate 真实 API） |
| 022 | ~~**Phase 7.3 组件 props 绑定不支持 fn 调用**~~ **✅ 已修复（auto-lang `64fb5e4d`）**：`expr_to_vue_bound_value` 加 `Expr::Call` 分支，`Component { prop: getQuestions(.msg) }` 现生成 `:prop="getQuestions(msg)"`（非 null）。a2vue golden 003_fn_call_prop 验证。auto-musk 已去掉 QuestionnaireMessage 包装组件，直接用 QuestionnaireCard + getQuestions fn 调用。 | `auto-lang crates/auto-lang/src/ui_gen/vue.rs`（expr_to_vue_bound_value Call 分支） |
| 022 | ~~**Phase 7.3 composable 声明自动调用**~~ **✅ 已修复（auto-lang `64fb5e4d`）**：use-block 语法扩展支持可选参数 `composable: useX(.arg) from "..."`。ExtImport 加 `call_args` 字段 + parser 解析 `(args)` + vue.rs 生成 `const x = useX(arg)`（空 args 仍 `useX()`，向后兼容无参 store composable）。单元测试 test_composable_with_args 验证带参 + 无参默认。auto-musk 的 useStreamingDocument 仍走 `fn:` 声明（逃生舱组件内部调用，无需 widget 层 composable）。 | `auto-lang crates/auto-lang/src/{ast/ui.rs,parser.rs,ui_gen/vue.rs}` |
| 022 | ~~**Phase 7c user 消息 mention 高亮未做**~~ **✅ 已修复（Phase 7.4）**：消息列表 user 分支现用 `UserMessage.vue` 逃生舱（v-html=renderMentions 高亮 @agent），不再走纯文本 `text .msg.content`。renderMentions helper 在 forge_helpers.ts。 | `src/front/components/UserMessage.vue` / `src/front/chats_view.at`（user 分支） |
| 022 | ~~**Phase 7c 逃生舱 ext 复制需显式声明**~~ **✅ 大部分缓解（Plan 028）**：Block 组相关显式声明已全部删除（forge_helpers/questionnaire/relay 改 .at fn 模块、StreamingRenderer/PrismCodeBlock/useStreamingDocument 改 platform 挂载自动复制、mention 由消费组件自带导入）。机制限制本身仍在：ext 复制只看 use 块，不递归 TS 内部相对 import——mention_helpers 等剩余 TS 的内部依赖仍需注意。 | `src/front/chats_view.at`（use 块）/ `auto-lang ui_gen` ext 复制机制 |
| 023 | ~~**StreamingRenderer 保留永久逃生舱**~~ **✅ 升格平台实现（Plan 028 T18/P1）**：不再以逃生舱形态被 ext 复制——.at 侧改协议声明 `component: Markdown from "platform:markdown"`，auto-man 注册表把 StreamingRenderer.vue + PrismCodeBlock.vue + useStreamingDocument 挂载到 gen `src/platform/`（相对导入改写 @/ext）。实现零改动，从「逃生舱」升格为「平台协议的 Vue 后端实现」；Rust/VM 后端未来可提供等价实现（comrak 等）。 | `src/front/components/StreamingRenderer.vue`（实现）/ `gen/front/vue/src/platform/markdown.vue`（挂载产物） |
| 023 | **逃生舱原生化行为差异**（Plan 023 P3 批量迁移 20 个组件，scoped 样式转全局 + 能力边界导致的轻微行为差异，均登记）：① RelayRunBox 日志自动滚底舍弃（template ref + nextTick watch 超能力边界，面板仍可手动滚动）；② SettingsMenu/SessionInfo/MentionDropdown 点击外部关闭舍弃（template ref 无能力表达，改按钮再次点击关闭）；③ `<transition>` fade 动画舍弃；④ GateCard approve 按钮自动聚焦舍弃；⑤ MentionInput mention 插入用 input 时捕获的 selectionStart（无 template ref 实时读取，中段插入场景有差异）；⑥ SessionInfo `code` 标签 → div 映射（视觉一致，语义元素丢失）；⑦ WikiNav 上传失败也会触发父树刷新（drop handler 命名 uploaded auto-emit，父刷新幂等）；⑧ SecretaryMessage review-in-specs 事件名连字符改 camelCase（父未接线该事件，无功能影响）。 | 各组件 .at（见 023-handoff.md §8 清单）/ 023-view-fn-component-codegen.md §3.6 |
| 022 | **`auto run` 后端生成绕过 a2r 核心**：`auto run`/`auto build` 的 `api.at → api.rs` 走 `auto-man/api_gen.rs:generate_api_rs`（手写 CRUD 模板），不调 `trans/rust.rs` a2r 转译器（仅 `db.at → db.rs` 经 `transpile_db_to_rs` 调用）。实测 `auth_login`：a2r 产物完整保留函数体 + use.rust；api_gen 产物坍缩成 `Vec<AuthUser>` CRUD。导致 `auto run` 无法一键拉起 auto-musk 全栈（前端 ✅ 后端 ❌）。解法在 auto-lang 侧：让 `generate_rust_server` 对 api.at 也走 `transpile_rust`。详见 plan 022 §8 D 类。 | `auto-lang crates/auto-man/src/api_gen.rs:1081`（generate_api_rs）/ `:301`（transpile_db_to_rs 唯一调 a2r 处）|

| 028 | **Block 组 Auto 化缺口 G1–G15 已全部解决**（F1–F9 特性落地 auto-lang：字典字面量/多参 if 条件/宿主 API/Regex 子集/Index v-model/withDefaults/v-html/SSE/HTTP/平台 markdown 协议）。**新残留**：① mention 域（renderMentions 族回调式 regex replace，F4 可移植子集外）留 mention_helpers.ts，归 G-对话壳/输入组立项；② a2ts 表达式优先级系统性问题（Bina 平铺丢括号、`/` 注入 Math.trunc 破坏浮点公式）已在 musk .at 源全部规避（中间变量），auto-lang 侧待立项；③ VM 渲染目标对 `store` facade（vue 合成的 `const store = reactive(...)`）报 Undefined variable 警告 + TS ext 依赖（injectStyles）显式 link 错误——VM store facade 概念缺失，归 VM 渲染目标立项。详见 [docs/plans/028-block-autolang-full-migration.md](028-block-autolang-full-migration.md)。 | `src/front/*.at` / `tmp/plan028-parity/`（148 项对拍） |

## 📋 未来增强

| Plan | 描述 | 参考 |
|---|---|---|
| 018 | **休眠镜像 full parity**：`tools.rs`/`spec_tools`/`orch_tools`/`server_serve` 等 ag 镜像为简化 dormant（description/schema 文本与 hw 有差异 + execute 依赖 extern stub）。计划 §10.6 Phase 4 评估 + §13 C 类已文档化为"设计内的等价镜像，非缺陷"。full parity 需 `.view` 手术 + 元数据对齐，留待后续接线计划。**注：Plan 019 已完成 `server_stream` 接线；Plan 020 Phase G/H 已完成 `relay_driver`(drive_run/loop/step 核心循环)+ `server_serve`(drive_* 已搬出,仅余 serve/settings_link 休眠骨架)的 full parity 接线——这两个模块不再属休眠镜像,现仅 `tools`/`spec_tools`/`orch_tools`/`server_serve` 休眠。Plan 021 §8 评估后明确这 4 镜像留待未来:激活成本极高(本地 trait Tool 不兼容 auto_ai_agent::Tool / orch_tools 缺 2 工具 / descriptions 缩水 / 需跨 auto-lang 改转译器)、收益为零(生产 agent 硬编码 hw tools 无切换路径)。** | `src/auto_generated/{tools,spec_tools,orch_tools,server_serve}.rs` |
| 018 | **HTTP 层测试缺口**（§13 E1）：`/api/run*`、`/api/chats/.../stream`、`/api/conversations/.../stream` 无 HTTP 层测试。**注：Plan 020 Phase D/E 已补 `/api/forge/*` + `/api/settings-link` 8 项；Plan 021 已补 `/api/files/*`(parity_files 5)+ workspace 端点(parity_workspace_endpoints 4:chats DELETE/drift/rebuild/related)+ config 端点(parity_config_endpoints 3:roles/app-config/harness,AUTOOS_HOME 隔离)。剩余 `/api/run*`/chats stream/conversations stream 仍无 HTTP 覆盖(plan 019 流式范围,逻辑层已覆盖)。** | — |
| 391 | **多段路径 codegen**：`std::env::var("X")`（多段 `::`）parser 现可解析，但 codegen 对小写模块段（`env`）仍发点（`std.env.var`）。单段 `env::var` + `use.rust std::env` 可用。多段需改 rust.rs:3097-3121 让小写段也认 `::`。 | `auto-lang trans/rust.rs:3097` |
| 029 | **D 组永久保留(架构性,非债务)**:① components/StreamingRenderer.vue + PrismCodeBlock.vue + composables/useStreamingDocument.ts——platform:markdown/代码块的 Vue 平台实现;② setup_auth_fetch.ts——App 引导 window.fetch monkey-patch(jwt+workspace 注入);③ composables/useT.ts——vue-i18n 宿主库桥(t() 工厂 + locale 读写);④ raw_upload.ts 瘦身版——XHR FormData 进度上传 + .text() 加载(Http.* 只展开 .json())。 | src/front/ 各文件 |
| 029 | **跨 store 接线桥(v1 单 store per file 限制)**:gate_router.ts(forge.current_gate/relay.gate_signal watch → GateInbox Register/Resolve)+ relay_command_runner.ts(RelayStore StartRun/AdvanceRun + Forge 推消息)——纯接线无状态,多 store 放开后可并入 .at。 | src/front/composables/ |
| 029 | **codegen 缺口(登记待修)**:① shadcn Button 映射丢弃动态 class/title 绑定(settings_menu 色板,已用容器子选择器 + :has(svg) 绕开);② store 内联模块 fn 的参数名若与 state 同名,生成 .value(字符串参数变 undefined——visual_store 参数改名规避,系统性风险在 ts_adapter state-ref 语境);③ fn 模块 try 内嵌 if 里的 return 仍不支持(parser 边界,Plan 022 已知);④ view 的 for-range 不支持直接调 fn(经 computed 中转)。 | auto-lang ui_gen |
| 029 | **迁移行为差异(均登记)**:① auto 主题的系统偏好 matchMedia change listener 不可表达,仅 Init/SetMode 读一次;② session copy 的 2s setTimeout 自动复位不可表达,✓ 保持至下次复制;③ 全局快捷键挂视图根 onkeydown(非 document 级)——目标元素均在应用内,等价。 | visual_store.at / session_info.at / app.at |
