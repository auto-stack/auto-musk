# Auto build metadata
.am/
.auto/

# a2vue generated frontend project (regenerated by `auto gen`)
# NOTE: 手写 Vue 组件若放进 gen/front/vue/src/，需单独跟踪——
# 目前手写文件还未放入，待 Step 4 接入时再决定跟踪策略。
gen/
# PLAN-041 T13 迁移物:gen 工程手写测试资产(重生成不覆盖)经 git add -f
# 单独跟踪(目录级排除使 ! 反选无效——已跟踪文件不受 ignore 影响):
#   gen/front/vue/src/__tests__/{i18n,frontmatter}.spec.ts + vitest-shim.d.ts
#   gen/front/vue/src/utils/frontmatter.ts

# a2vue codegen by-products (Plan 022): api client + rust backend scaffolding
# emitted by `auto build` from src/front + src/back .at. Regenerated each build.
dist/
examples/
# PLAN-038: vendored @autodown/vue dist 快照入库（file: 依赖目标,须可提交）
!vendor/@autodown/vue/dist/
!vendor/@autodown/vue/dist/**

# Node / pnpm
node_modules/
.pnpm-store/

# Built config-page ESM bundle (regenerated by `npm run build` in frontend/)
**/frontend-dist/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Rust build output (backend)
backend/target/
**/target/

# File-tool unit-test fixtures (crate-local .test-tmp/ under backend/crates)
**/.test-tmp/

# ZCode / Claude agent working dirs
.claude/
.zcode/

# Playwright debug artifacts (screenshots, debug scripts, temp responses)
web/debug-shots/
web/debug-*.mjs
web/*cookies.txt
web/*login-resp.txt
web/*relay-resp.txt

# Accidental Windows "nul" device-name files + temp greeting output
nul
web/nul
greeting.txt

# Agent tool-testing scratch files (write_file demos, pie-chart demo, etc.)
backend/hello.py
backend/my-demo.py

# Superpowers brainstorming visual-companion mockups
.superpowers/
backend/.autoos/
tmp/

# a2r transpiled output (regenerated by `auto trans` from .at sources)
**/*.a2r.rs

# runtime/debug logs (Plan 022 runtime fixes)
*.log
*.err
.playwright-cli/
.worktrees/
track-switch-diff-*.html
